version: 2

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - build
      - deploy-dev:
          requires:
            - build
          filters:
            branches:
              only: main
        

jobs:
 build:
   machine:
    image: ubuntu-2004:current
   steps:
     - checkout
     - run: |
         echo "$DOCKER_PASS" | docker login --username $DOCKER_USER --password-stdin

     # build the application image
     - run: TAG="0.1.${CIRCLE_BUILD_NUM}-${CIRCLE_BRANCH}" && docker build -t shantanu2307/devops_app:$TAG . && docker push shantanu2307/devops_app:$TAG
 deploy-dev:
    machine: true
    steps:
      - run:
          name: Build for Heroku
          staging:
          command:
            - docker login --email=$HEROKU_LOGIN --username=$HEROKU_LOGIN --password=$HEROKU_TOKEN registry.heroku.com
            - docker build --rm=false -t registry.heroku.com/salary-predictor-nsut/web .
            - docker push registry.heroku.com/salary-predictor-nsut/web:latest
      - run :
          name: Deploy to Heroku
          command: heroku container:release web -a salary-predictor-nsut
